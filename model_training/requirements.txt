torch>=2.4
numpy>=1.26
onnx>=1.16          # required by torch.onnx.export
onnxruntime>=1.18   # optional: verifies the exported graph

# The backend API is read over urllib from the standard library, so there is no
# HTTP client here and no database driver -- the pipeline only needs the API up.
